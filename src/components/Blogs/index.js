import React, { Component } from 'react';
import "./Blogs.scss";

const Blogs = props => {
    return (
        <section id="Blogs" className="blogsSection">
            Writing in progess !!!!
        </section>

    )
};

export default Blogs;
